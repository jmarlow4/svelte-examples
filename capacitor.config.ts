import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.org.svelte',
  appName: 'svelte-examples',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
